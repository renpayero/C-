#include <iostream>

using namespace std;

int main()
{
   /* Considerar la definición de función:
    void dos(int a[], int cuantos) {
    for (int indice = 0; indice < cuantos; indice++) {
    a[indice] = 2;
    }
    }
    ¿Cuales de las siguientes son llamadas de función aceptables?
    int mi_arreglo[29];     NO
    dos(mi_arreglo, 29);    SI
    dos(mi_arreglo[], 29);  NO
    dos(mi_arreglo, 10);    SI
    dos(mi_arreglo, 55);    SI
    int tu_arreglo[100];    NO
    dos(tu_arreglo, 100);   SI
    dos(mi_arreglo[3], 29); NO
    */
}
